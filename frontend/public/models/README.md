# Avatar model folder

Put one of these files here:

- `avatar.vrm` for VRoid / VRM mode
- `model.pmx` plus its texture folders for MMD mode

The Expo web dev server serves files in `frontend/public` from the web root.
